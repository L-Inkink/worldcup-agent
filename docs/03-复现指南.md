# 世界杯冠军预测 Agent —— 复现指南

> 本文档提供完整的算法公式、数据源细节、Prompt 模板与实现步骤。**任何人（或任何 AI 编程工具）按本文档 + [02-系统架构.md](./02-系统架构.md) 的接口契约即可独立复现本工程**，无需参考原始开发过程。

## 1. 环境要求

| 组件 | 版本 | 用途 |
|-----|------|-----|
| Python | ≥ 3.11 | 后端 |
| Node.js | ≥ 20 | 前端构建 |
| Docker | ≥ 24 | 部署（可选） |

后端依赖（requirements.txt）：`fastapi`、`uvicorn`、`httpx`、`numpy`、`scipy`、`pydantic`、`openai`（DashScope 兼容客户端）、`pytest`。
前端依赖：`vue@3`、`vite`。不引入图表库（SVG 手绘）。

环境变量（均可缺省，缺省时走降级路径）：

```bash
DASHSCOPE_API_KEY=       # 阿里云百炼控制台获取（缺省→规则模板解释）
FOOTBALL_DATA_TOKEN=     # 备用数据源（缺省→Wikipedia 主源 + 快照兜底）
AUTO_REFRESH_MINUTES=30  # 自动刷新间隔（0=关闭）
```

## 2. 数据源

### 2.1 赛程与赛果 — Wikipedia（主源，无需 key）

通过 MediaWiki API 拉取 wikitext 并确定性解析（不要用 LLM 提取，易幻觉）：

| 页面 | 解析内容 |
|-----|---------|
| `Template:2026 FIFA World Cup group tables` | 12 组积分（`team_order` 已含官方同分判定） |
| `2026 FIFA World Cup Group A..L`（12页） | 72 场小组赛比分（`{{score link\|..\|2–0}}`） |
| `2026 FIFA World Cup knockout stage` | RoundN 对阵树模板：全部淘汰赛对阵/比分/加时/点球 |

解析要点：先剥 HTML 注释 → 折叠 `[[a|b]]` 链接 → 旗帜模板替换占位符 → 再按 `|` 切分（嵌套模板内含 `|`，直接切分会错位）。请求需带 UA、全局 1s 间隔、429/网络错误指数退避重试。**同一解析逻辑随赛事推进自动带出新完赛结果**——这是 auto-refresh 的基础。

### 2.1b 备用源 — football-data.org（可选）

```
GET https://api.football-data.org/v4/competitions/WC/matches
Header: X-Auth-Token: {FOOTBALL_DATA_TOKEN}
```

免费层限流 10 次/分钟。仅在配置 token 时作为交叉验证/备用。

### 2.2 Elo 评分 — eloratings.net

World Football Elo Ratings（http://eloratings.net），页面数据可通过其 TSV 接口 `http://eloratings.net/World.tsv` 获取（若结构变化则退化为手工整理快照）。取每队当前 Elo 分。

### 2.3 FIFA 排名

FIFA 官网排名页数据量小且变化慢，直接整理为静态 JSON `backend/data/fifa_rankings.json` 随仓库提交（数据版本注明日期）。

### 2.4 快照机制（关键容错）

- Collector 每次成功拉取后，将原始响应规范化并写入 `backend/data/{matches,elo}.json`，附 `fetched_at` 时间戳。
- 任何网络失败 → 读取快照，输出中标记 `data_source: "snapshot"`。
- 仓库必须提交一份可用快照，保证 clone 后零配置可跑。

## 3. 核心算法（Predictor / Rating / Simulator）

### 3.1 综合实力分（rating.py）

对每队计算综合实力分（单位与 Elo 同量纲）：

```
strength = elo
         + w_fifa   * fifa_bonus        # fifa_bonus = (50 - fifa_rank) * 2，上限100（排名越前加分越多）
         + w_form   * recent_form       # 近10场：胜+1/平0/负-1 求和 * 10
         + w_wc     * wc_performance    # 本届场均净胜球 * 30（体现当前状态）
默认权重：w_fifa = 0.3, w_form = 0.5, w_wc = 1.0
```

说明：Elo 本身已是最强信号，其余为微调项。以上为经验默认值，**会被 `data/model_params.json`（校准产物，见 §3.4）覆盖**——v1.2 校准结果 w_form=1.0。`recent_form` 使用 eloratings TSV 的近一年评分变化列作为动量代理。

### 3.2 单场比分概率 —— 独立泊松模型（predictor.py）

足球预测经典方法（Maher, 1982）。步骤：

**① 实力差 → 主队胜率期望（Elo 期望公式）**

```
d = strength_A - strength_B          # 中立场地，无主场加成（世界杯淘汰赛）
E_A = 1 / (1 + 10^(-d/400))          # A 队"期望得分"，即 Elo 胜率期望
```

**② 期望得分 → 双方期望进球 λ**

世界杯场均总进球约 2.7（BASE_GOALS = 2.7）：

```
λ_A = BASE_GOALS * E_A ^ 0.85 * 0.9      # 强队期望进球
λ_B = BASE_GOALS * (1-E_A) ^ 0.85 * 0.9
```

指数 0.85 为平滑项；系数 0.9 为校准项，使等实力时 λ_A + λ_B 恰为 2.7（推导：2 × 0.5^0.85 × 0.9 ≈ 1.0）。该校准由单元测试 `test_equal_strength_balanced` 守护。

**③ 泊松比分矩阵**

```
P(比分 = i:j) = Poisson(i; λ_A) * Poisson(j; λ_B)    i, j ∈ [0, 8]
```

由矩阵汇总：`p_win = Σ P(i>j)`，`p_draw = Σ P(i=j)`，`p_loss = Σ P(i<j)`；`most_likely_score = argmax P(i,j)`。

**④ 淘汰赛晋级概率（平局 → 加时/点球）**

```
p_advance_A = p_win + p_draw * (0.5 + (E_A - 0.5) * 0.4)
```

即平局后进入加时/点球，强队仍略占优但优势衰减（点球高度随机）。加时后比分展示用 `most_likely_score` 附"(加时/点球)"标记。

### 3.3 逐轮推演与蒙特卡洛（simulator.py）

**确定性推演**（页面主展示）：

```
for round in [32强, 16强, 8强, 4强, 决赛]:
    for match in round:
        if match.status == FINISHED: 采用真实结果
        else: 晋级方 = p_advance 较高一方；展示比分 = most_likely_score
    下一轮对阵 = 按官方赛程树的晋级槽位填充
```

**蒙特卡洛模拟**（夺冠概率分布）：

```
rng = numpy.random.default_rng(seed=2026)     # 固定种子，保证可复现
repeat 10000 次:
    从当前真实赛况出发，每场未赛比赛按 p_advance 伯努利采样晋级方
    记录每队所到达的最远轮次
p_champion[team] = 夺冠次数 / 10000（p_final、p_semi 同理）
```

**回测指标**（可信度证据）：对所有已赛场次，用赛前数据跑 Predictor，统计"胜平负方向命中率"与"晋级方命中率"，写入 `model_backtest` 字段。

### 3.4 回测驱动参数校准（calibrate.py，v1.2）

`python -m agent.calibrate --apply` 在全部已赛场次上做网格搜索（目标：平均对数损失，
预测用赛前实力分防泄漏），搜索空间：goal_exp × rho(Dixon-Coles) × elo_div × w_form。
改善超过 0.005 才写入 `data/model_params.json`（predictor 与 rating 启动时自动加载，
缺省回退经验默认值）。2026-07-05 实测：log loss 0.681 → 0.631，
最优 {goal_exp: 1.0, rho: -0.12, elo_div: 320, w_form: 1.0}。

Dixon-Coles 修正（低比分单元格）：
```
P(0,0) *= 1 - λμρ；P(0,1) *= 1 + λρ；P(1,0) *= 1 + μρ；P(1,1) *= 1 - ρ；然后整矩阵重归一
```

## 4. Qwen 推理解释（reasoner.py）

### 4.1 接入方式

DashScope 的 OpenAI 兼容接口：

```python
from openai import OpenAI
client = OpenAI(api_key=os.environ["DASHSCOPE_API_KEY"],
                base_url="https://dashscope.aliyuncs.com/compatible-mode/v1")
# 单场解说 model="qwen-plus"；冠军长文 model="qwen-max"
```

### 4.2 单场解说 Prompt 模板（qwen-plus）

```
系统：你是一名专业足球分析师。基于给定的量化数据，用中文写一段120字以内的
比赛预测分析。只解释数据，不得更改任何数字结论，不得编造数据中没有的信息。

用户：比赛：{home} vs {away}（{round}）
量化依据：
- 综合实力分：{home} {s_home}（Elo {elo_home}，FIFA第{rank_home}，近10场{form_home}，本届场均净胜{gd_home}）
             {away} {s_away}（同上格式）
- 模型输出：{home}胜率{p_win}%，平局{p_draw}%，{away}胜率{p_loss}%
- 最可能比分：{score}；晋级概率：{home} {p_advance}%
请解释为什么模型给出这一预测。
```

### 4.3 冠军推理长文 Prompt（qwen-max）

```
系统：你是一名资深足球战术与数据分析专家。基于统计模型的完整推演结果撰写
冠军预测报告（600-800字，中文），结构：①夺冠热门格局 ②预测冠军的晋级路径
逐轮分析 ③主要威胁 ④结论。只解释数据，不得更改数字结论。

用户：蒙特卡洛10000次模拟夺冠概率Top8：{odds_table}
预测冠军：{champion}（概率{p}%）
其晋级路径与各场预测：{path_details}
模型对已赛场次回测命中率：{backtest}%
```

### 4.4 降级模板

无 key/调用失败时，用 f-string 规则模板生成同结构文本（例："综合实力分 X 高出对手 Y 分，模型给出 Z% 胜率，主要来自 Elo 优势与本届净胜球表现……"），标记 `reasoning_source: "template"`。

## 5. 实现步骤（建议顺序）

按依赖顺序实现，每步有独立验收点：

| 步骤 | 内容 | 验收 |
|-----|------|-----|
| 1 | 项目骨架 + requirements + `teams.py` 静态元数据 | 目录结构与架构文档一致 |
| 2 | `collector.py`（Wikipedia 解析 + 快照降级 + 重试） | 断网时仍能返回完整 TournamentData |
| 3 | `rating.py` | 单测：权重计算正确 |
| 4 | `predictor.py`（泊松 + DC 修正 + 参数注入） | 单测：概率求和=1；对称性；DC 提升平局概率 |
| 5 | `simulator.py`（推演 + 蒙特卡洛 + 回测） | 单测：固定种子可复现；已赛结果锁定 |
| 6 | `reasoner.py`（Qwen + 模板降级） | 无 key 时输出模板解释 |
| 7 | `pipeline.py` + `api.py`（含 auto-refresh） | `python -m agent.pipeline` 产出合法 prediction.json；所有 API 路由 200 |
| 8 | 前端四视图 | `npm run build` 成功；对阵树正确渲染已赛/预测场次 |
| 9 | `features.py`（休息天数/点球/核心球员） | 合成对阵树单测通过；facts 中出现上下文行 |
| 10 | `calibrate.py`（回测网格搜索） | log loss 显著优于随机基线 ln3；改善超阈值落盘 |
| 11 | Dockerfile + 部署脚本 | `docker run` 拉起完整服务 |
| 12 | 阿里云 ECS 部署 | 公网 URL 可访问 |

## 6. 验收清单（对照赛题）

- [x] 数据采集：Wikipedia 实时采集可演示，快照降级可演示（拔网线测试）
- [x] 分析决策：小组赛分析（积分表+出线）、淘汰赛逐层推演、决赛预测全链路
- [x] 可解释性：每场预测可点开查看量化依据（含休息天数/点球记录/核心球员）+ 推理文本
- [x] 结果输出：冠军 + 各轮比分预测 + 夺冠概率分布 + 推理长文
- [x] 可视化：对阵树/小组表/概率榜/推理页四视图（浏览器实测通过）
- [ ] 加分项1：阿里云公网可访问（脚本与文档就绪，待 ECS 实操）
- [ ] 加分项2：推理链路使用 qwen-plus / qwen-max（代码就绪，待配置 DASHSCOPE_API_KEY 实测）
- [x] 可信度：已赛场次回测命中率展示（90 场 73.3%，赛前口径无泄漏）
- [x] 可复现：固定随机种子、快照随仓库提交、26 个单测通过
- [x] 自我校准：回测网格搜索，log loss 0.681→0.631，参数落盘可追溯
- [x] 时效性：auto-refresh 自动吸收新赛果（7/4 两场 16 强赛前预测全中）
